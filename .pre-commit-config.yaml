# Format-only pre-commit. Never blocks commits on lint or type errors —
# just keeps diffs tidy. See AGENTS.md for the rationale.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff-format
